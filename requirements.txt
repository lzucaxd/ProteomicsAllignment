# Python >= 3.10 (matches pyproject.toml / harmonize package)
# Install: pip install -r requirements.txt
# Editable package (same deps declared in pyproject.toml): pip install -e .
requests>=2.28.0

pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
scikit-learn>=1.1.0
scipy>=1.9.0
statsmodels>=0.13.0
pyyaml>=6.0

# For reading .xlsx (e.g. data/ccle_peptide/check.py, Table_S2)
openpyxl>=3.0.0
