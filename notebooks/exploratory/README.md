# Exploratory notebooks

Notebooks here are **not** part of the reproducible benchmark pipeline. They are for ad hoc analysis and may assume local paths or extra data.

**Pipeline notebooks** (if any) should live under a clearly named subfolder and be referenced from `docs/` or `HANDOFF.md`.

Do not commit large outputs or secrets inside notebooks; use relative paths under the repo root.
